name: realtime_audio
description: "Audio package to handle streaming chunk playback & recording to use with realtime APIs like OpenAI Realtime, Elevenlabs, HumeAI Voice and others."
version: 0.0.10
homepage: "https://github.com/volskaya/realtime_audio.flutter"
topics:
  - ai
  - audio
  - openai
  - elevenlabs
  - hume

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.0
  json_annotation: ^4.9.0
  uuid: ^4.5.1
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  build_runner: ^2.4.13
  freezed: ^2.5.0
  json_serializable: ^6.8.0

flutter:
  plugin:
    platforms:
      android:
        package: dev.volskaya.realtime_audio
        pluginClass: RealtimeAudioPlugin
      ios:
        pluginClass: RealtimeAudioPlugin
      macos:
        pluginClass: RealtimeAudioPlugin
